#include "myC.h"
#include <stdio.h>
int main()
{
    int a=4;
    char b=6;
    int f=9;
    float c=2.0;
    float f = 9.0;
    // Type Error: Redecleared Variable f

	// Writing if statement without else if
    
    d = a + 3; // Type Error: Undefined Variable d && Type mismatch for the two side operands of an assignment statement

    if(c==1)
	{
        printf("MAX: %d\n",MAX);
	}

	else
	{
			printf("END");
	}

    e = a * b; // Type Undefined Varable e && Type mismatch for the two side operands of an assignment statement && Type                         missmatch for the operator * in an expression

	return 1;
}
